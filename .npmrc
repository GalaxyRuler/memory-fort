# onnxruntime-node 1.22.0 postinstall tries to fetch CUDA/GPU providers and its
# Linux path is broken (microsoft/onnxruntime#24918, #24770). We ship CPU-only;
# the CPU binaries are bundled in the npm tarball, so skip the download entirely.
onnxruntime-node-install=skip
