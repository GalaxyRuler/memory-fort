# Memory Fort v1.1 Credibility Evidence

Date: 2026-06-06

## Commands

| Area | Command | Result | Notes |
| --- | --- | --- | --- |
| Retrieval eval | `npm run memory -- eval-retrieval --limit 20 --json` | Passed | Used existing `dist/cli.mjs`; no build was needed before eval. |
| Gold set provenance check | Parsed `qa/retrieval-gold.jsonl` by `type` | Passed | Gold set has 8 `provenance` rows, so no expansion was made. The required `--limit 20` eval window contains fact, causal, and dependency rows only because of current file ordering. |
| Package proof | BOM-safe `npm pack --dry-run --json` PowerShell parse | Passed | See Package Surface. |
| Leak scan | `npm run scan:leaks` | Passed | Required because release evidence docs are in the scan boundary. |
| Integration CLI surface | `npm run memory -- install <platform> --help` for `claude-code`, `codex`, `antigravity`, `opencode` | Passed | Confirms local CLI install surface is present; this is not live install or smoke proof. |
| Focused tests accumulated before Task 8 | Recent branch commit bodies inspected with `git log -n 18 --format=...` | Not claimed | Commit subjects describe search/provenance fixes, but no command-output bodies were present to summarize as test evidence. |
| Final gate, Task 10 | `npm run typecheck` | TBD | Reserved for Task 10 final gate. |
| Final gate, Task 10 | `npm run build` | TBD | Reserved for Task 10 final gate. |

## Retrieval Evaluation

Command:

```powershell
npm run memory -- eval-retrieval --limit 20 --json
```

Summary:

```json
{
  "questionCount": 20,
  "recall": {
    "5": { "withGraph": 0.875, "withoutGraph": 0.675 },
    "10": { "withGraph": 0.9, "withoutGraph": 0.725 }
  },
  "graphLift": {
    "5": 0.2,
    "10": 0.175
  },
  "mrr": {
    "withGraph": 0.6667,
    "withoutGraph": 0.4717
  },
  "byType": {
    "fact": {
      "questionCount": 8,
      "recall": {
        "5": { "withGraph": 0.875, "withoutGraph": 0.625 },
        "10": { "withGraph": 0.875, "withoutGraph": 0.625 }
      },
      "graphLift": {
        "5": 0.25,
        "10": 0.25
      },
      "mrr": {
        "withGraph": 0.6458,
        "withoutGraph": 0.4583
      }
    },
    "causal": {
      "questionCount": 8,
      "recall": {
        "5": { "withGraph": 1, "withoutGraph": 0.875 },
        "10": { "withGraph": 1, "withoutGraph": 0.875 }
      },
      "graphLift": {
        "5": 0.125,
        "10": 0.125
      },
      "mrr": {
        "withGraph": 0.7917,
        "withoutGraph": 0.5521
      }
    },
    "dependency": {
      "questionCount": 4,
      "recall": {
        "5": { "withGraph": 0.625, "withoutGraph": 0.375 },
        "10": { "withGraph": 0.75, "withoutGraph": 0.625 }
      },
      "graphLift": {
        "5": 0.25,
        "10": 0.125
      },
      "mrr": {
        "withGraph": 0.4583,
        "withoutGraph": 0.3375
      }
    }
  }
}
```

Graph lift:

| K | With graph | Without graph | Lift |
| --- | ---: | ---: | ---: |
| 5 | 0.875 | 0.675 | 0.200 |
| 10 | 0.900 | 0.725 | 0.175 |

Gold set decision:

| Check | Result |
| --- | --- |
| `qa/retrieval-gold.jsonl` total rows parsed | 41 |
| Provenance rows in gold set | 8 |
| `qa/retrieval-gold.jsonl` changed in Task 8 | No |
| Reason | Existing gold set already has at least 5 provenance questions. No live private vault paths were added. |

## Package Surface

PowerShell proof shape:

```powershell
$raw = npm pack --dry-run --json
$text = ($raw -join "`n").TrimStart([char]0xFEFF)
$data = $text | ConvertFrom-Json
$pkg = $data[0]
[pscustomobject]@{ name = $pkg.name; version = $pkg.version; fileCount = $pkg.files.Count; packageSize = $pkg.size; unpackedSize = $pkg.unpackedSize }
```

Result:

| Field | Value |
| --- | ---: |
| name | `memory-fort` |
| version | `0.1.2` |
| fileCount | 124 |
| packageSize | 4,098,715 bytes |
| unpackedSize | 17,317,678 bytes |

## Integration Certification

| Integration | Local command evidence | Live smoke status | Release note |
| --- | --- | --- | --- |
| Claude Code | `npm run memory -- install claude-code --help` passed and lists `claude-code` in supported platforms. | Pending operator/live smoke. | CLI surface present; no live installer write or agent smoke was run in Task 8. |
| Codex | `npm run memory -- install codex --help` passed and lists `codex` in supported platforms. | Pending operator/live smoke. | CLI surface present; no live installer write or agent smoke was run in Task 8. |
| Antigravity | `npm run memory -- install antigravity --help` passed and lists `antigravity` plus `--surface <surface>`. | Pending operator/live smoke. | CLI surface present; no live installer write or agent smoke was run in Task 8. |
| OpenCode | `npm run memory -- install opencode --help` passed and lists `opencode` in supported platforms. | Pending operator/live smoke. | CLI surface present; no live installer write or agent smoke was run in Task 8. |

## Residual Pending Items

| Item | Status |
| --- | --- |
| Task 10 typecheck final gate | TBD |
| Task 10 build final gate | TBD |
| Live Claude Code install and smoke | Pending operator/live environment |
| Live Codex install and smoke | Pending operator/live environment |
| Live Antigravity install and smoke | Pending operator/live environment |
| Live OpenCode install and smoke | Pending operator/live environment |
