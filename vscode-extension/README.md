# Memory Fort VS Code Extension

Registers the `@memory` chat participant and writes captured VS Code chat turns to the local Memory Fort vault.

The stable VS Code Chat Participant API exposes requests addressed to this participant. If a future VS Code build exposes broader chat-observation events, the extension subscribes to them opportunistically without requiring proposed APIs.
